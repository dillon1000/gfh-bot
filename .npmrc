# Allow build scripts for native dependencies
# pnpm 11+ blocks these by default as a safety measure
ignore-scripts=false

# Ensure proper dependency resolution
auto-install-peers=true
shamefully-hoist=true
